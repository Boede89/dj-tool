version: '3.8'

services:
  dj-tool:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    env_file:
      - .env
    volumes:
      - ./database.db:/app/database.db
    restart: unless-stopped
